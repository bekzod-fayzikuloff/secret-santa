import React from 'react'
import { IUser } from '../../pages/BoxPage'

const MainBoxPage = (props: { user: IUser }) => {
  // const { user } = props;
  return <div>foo</div>
}

export default MainBoxPage
